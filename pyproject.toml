[project]
name = "malazan-companion"
version = "0.1.0"
description = "Spoiler-safe reading companion prototype"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
malazan = "malazan_companion.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
